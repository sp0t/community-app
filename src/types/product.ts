export type Product = {
  rank: number;
  image: string;
  name: string;
  messages:number;
  constribution:string;
  // category: string;
  // price: number;
  // sold: number;
  // profit: number;
};
